import restar from './util.js';
//se usa la palabra import para traer la funcion restar del archivo util.js

console.log(restar(5,2));
console.log(restar(5,3));
console.log(restar(10,4));
//con los console log es para asignar valores a la funcion restar y mostrar el resultado en la consola.